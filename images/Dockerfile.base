FROM ghcr.io/void-linux/void-glibc:latest
LABEL org.opencontainers.image.description="basic agents"
RUN xbps-install -Sy git curl ca-certificates base-devel && \
    rm -rf /var/cache/xbps/*
COPY nac /usr/local/bin/nac
RUN chmod +x /usr/local/bin/nac
WORKDIR /workspace
CMD ["nac"]
